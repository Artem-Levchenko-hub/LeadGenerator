sqlalchemy>=2.0.35
# httpx[socks] = httpx + поддержка SOCKS5-прокси (нужно для VPS-RU → Anthropic API).
httpx[socks]>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
openpyxl>=3.1.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
tenacity>=9.0.0

# Веб-приложение
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=23.0.0
jinja2>=3.1.4
python-multipart>=0.0.12
itsdangerous>=2.2.0
bcrypt>=4.2.0

# SSH-деплой (только для разработчика)
paramiko>=3.5.0

# === Агентная студия (worker + agents) ===
anthropic>=0.40.0          # SDK для Claude. Уважает ANTHROPIC_BASE_URL для прокси.
apscheduler>=3.10.4        # Фоновые задачи воркера (cron-like).
aiosmtplib>=3.0.2          # Async SMTP для UniSender отправки.
dnspython>=2.6.1           # Проверка SPF/DKIM/MX в Outreach Agent.
python-whois>=0.9.4        # WHOIS-lookup для анализа домена.
reportlab>=4.2.5           # Рендеринг PDF (КП).
aiogram>=3.13.0            # Telegram-бот.
